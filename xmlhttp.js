function fn_get(url, dest){    //instantiate    var req = new XMLHttpRequest();    //handler    req.onload = function ()    {        //debug//        console.log(req.readyState, req.statusText);//        console.log(req.responseText);//        console.log(req.responseXML);        dest.innerHTML = req.responseText;        req = null;    };    //send    req.open("GET", url, true);    req.setRequestHeader("Content-Type", "application/x-www-form-urlencoded");  //important    req.send();}//requestfunction fn_send(method, url, dest, params){    //instantiate    var req = new XMLHttpRequest();    //handler    req.onload = function ()    {        //debug        console.log(req.readyState, req.statusText);        console.log(req.responseText);        dest.innerHTML = req.responseText;        req = null;    };    //send    req.open(method, url, true);    req.setRequestHeader("Content-Type", "application/x-www-form-urlencoded");  //important    req.send(params);}function fn_submit(form, action, dest){    //name-value pairs    var params = new Array();    //controls    var cc = form.getElementsByTagName('input');    //iterate controls    for (var i = 0; i < cc.length; i++)    {        params.push(cc[i].id + "=" + encodeURIComponent(cc[i].value));    }    //debug    console.log(form.id, params.join("&"));    //send reqest    fn_send("POST", action, dest, params.join("&"));}